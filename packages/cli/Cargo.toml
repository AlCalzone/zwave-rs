[package]
name = "cli"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
hex = "0.4.3"
zwave-serial = { path = "../serial" }
zwave-driver = { path = "../driver" }
tokio = { version = "1.33.0", features = ["rt-multi-thread", "macros"] }
